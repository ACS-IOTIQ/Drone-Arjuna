# Test-only dependencies — install inside the backend container:
#   pip install -r requirements-test.txt
#
# Core test stack
pytest>=8.0
pytest-asyncio>=0.23      # asyncio_mode = auto support
anyio[asyncio]>=4.0       # async backend for httpx + pytest-asyncio

# SQLite async driver (replaces PostgreSQL for unit tests)
aiosqlite>=0.20.0

# httpx is already in requirements.txt (0.27.0) — listed here for clarity
# httpx>=0.27.0
