# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop==0.19.0          # Drop-in asyncio loop replacement (2-4x faster I/O)

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0         # Async PostgreSQL driver
alembic==1.13.1         # Migrations

# Redis
redis[hiredis]==5.0.4
aioredis==2.0.1

# RabbitMQ
aio-pika==9.4.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1              # Pin to 4.0.1 — passlib is incompatible with 4.1+
python-multipart==0.0.9

# MAVLink
pymavlink==2.4.41
pyserial==3.5

# Geospatial
shapely==2.0.4
pyproj==3.6.1

# HTTP client (for internal calls)
httpx==0.27.0

# Object storage
boto3==1.34.101         # S3-compatible (MinIO)

# Utilities
python-dotenv==1.0.1
structlog==24.1.0       # Structured JSON logging
orjson==3.10.3          # Fast JSON serialisation