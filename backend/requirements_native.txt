# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0         # Async PostgreSQL driver
alembic==1.13.1         # Migrations

# Redis
redis[hiredis]==5.0.4
aioredis==2.0.1

# RabbitMQ
aio-pika==9.4.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1              # Pin to 4.0.1 — passlib is incompatible with 4.1+
python-multipart==0.0.9

# MAVLink
pymavlink==2.4.41
pyserial==3.5
pynmea2==1.19.0

# Geospatial
shapely==2.0.4
pyproj==3.6.1

# Fleet assignment optimisation (Q-SWARM port — classical solver, always required)
ortools==9.10.4067
numpy==1.26.4

# Quantum path for fleet assignment (use_quantum=True) — QAOA on Qiskit Aer

# HTTP client (for internal calls)
httpx==0.27.0

# Object storage
boto3==1.34.101         # S3-compatible (MinIO)

# Utilities
python-dotenv==1.0.1
structlog==24.1.0       # Structured JSON logging
orjson==3.10.3          # Fast JSON serialisation

# Search
elasticsearch[async]==8.12.0

# Email
aiosmtplib==5.1.2

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
anyio==4.3.0
aiosqlite==0.20.0
PyYAML==6.0.1          # config/infra tests parse docker-compose.yml and ci.yml
